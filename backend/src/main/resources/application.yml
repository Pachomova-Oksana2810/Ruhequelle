spring:
  config:
    import: optional:classpath:application-local.yml
  application:
    name: Ruhequelle
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true&connectionTimeZone=UTC
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: update
      database-platform: org.hibernate.dialect.MySQL8Dialect
      show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: koldakova.anna88@gmail.com
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${PORT:8080}
  address: 0.0.0.0

# Уведомления при новой записи: Email + Telegram
# Для Telegram: создайте бота через @BotFather, получите токен.
# Chat ID: напишите боту @userinfobot или своему боту /start — он покажет ваш chat_id.
notification:
  email:
    enabled: true
    to: koldakova.anna88@gmail.com
  telegram:
    enabled: true
    bot-token: ${TELEGRAM_BOT_TOKEN:}
    chat-id: ${TELEGRAM_CHAT_ID:}